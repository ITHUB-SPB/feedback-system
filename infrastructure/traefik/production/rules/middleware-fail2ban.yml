http:
  middlewares:
    custom-fail2ban:
      plugin:
        fail2ban:
          allowlist:
              ip: "::1,127.0.0.1"
          rules:
              bantime: "3h"
              enabled: true
              findtime: "10m"
              maxretry: 4
              statuscode: "400,401,403-499"
