http:
  middlewares:
    chain-authelia:
      chain:
        middlewares:
          # - middleware-custom-fail2ban
          - middleware-rate-limit
          - middleware-secure-headers
          - middleware-authelia